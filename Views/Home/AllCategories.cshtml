@model AllCategoriesWrapper

<div class="col-sm-6 text-right">
                <a href="/dashboard">Dashboard</a> | <a href="/logout">Log Out</a>
            </div>



 <div class="row">
            <div class="col-sm-10 offset-sm-1">
                <table class="table">
                    <thead>
                        <th>Category Name</th>
                        <th>Discription</th>
                        <th>Action(s)</th>
                    </thead>
                    <tbody>
                        @foreach(Category cat  in Model.AllCategories)
                        {
                            <tr>
                                <td>@cat.CategoryName</td>
                                <td>@cat.Discription </td>
                                <td> placeholder   </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>